# ArbMaster Pro - Process Configuration
# Railway will use this to determine how to run the application

# Main trading bot (default)
web: python src/main.py run --dry-run

# Alternative: Run the Streamlit dashboard
# web: streamlit run dashboard/app.py --server.port $PORT --server.address 0.0.0.0

# Worker process for background scanning (if using separate processes)
# worker: python src/main.py run --dry-run
